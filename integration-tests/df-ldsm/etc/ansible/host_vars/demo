---

# This file supplies the variables for a particular host.
# For real environments, this file should be encrypted using git-crypt, so it
# is safe to store secrets such as passwords in it and then commit the file in a
# Git repository

ip_address: '192.168.34.66'

airflow_db_admin_password: 'MySecretPassword'
airflow_db_password: 'airflowdb'

mri_db_user: mri
mri_db_password: mripwd
mri_db_admin_password: "{{ airflow_db_admin_password }}"

slack_token: ''

ldsm_db_password: "ldsm-db"
ldsm_db_admin_password: "ldsmroot"
