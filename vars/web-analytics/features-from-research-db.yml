---

# Use the features data from Research database

portal_backend_features_env:
  SCIENCE_DB_URL: "{{ research_db_jdbc_url }}"
  SCIENCE_DB_SCHEMA: "public"
  SCIENCE_DB_MAIN_TABLE: "{{ portal_backend_features_table }}"
  SCIENCE_DB_USER: "{{ research_db_user }}"
  SCIENCE_DB_PASSWORD: "{{ research_db_password }}"
