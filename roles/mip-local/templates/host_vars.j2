---

# This file supplies the variables for a particular host.
# For real environments, this file should be encrypted using git-crypt, so it
# is safe to store secrets such as passwords in it and then commit the file in a
# Git repository

fqdn: "{{ server_dns }}"
hostname: "{{ server_hostname }}"
ip_address: "{{ server_ip_address }}"

slack_token: ''

ldsm_db_password: "{{ ldsm_db_password }}"
ldsm_db_admin_password: "{{ ldsm_db_admin_password }}"

research_db_password: "{{ research_db_password }}"
research_db_admin_password: "{{ research_db_admin_password }}"

meta_db_password: "{{ meta_db_password }}"
woken_db_password: "{{ woken_db_password }}"
portal_db_password: "{{ portal_db_password }}"

meta_db_docker_registries:
  - url: registry.gitlab.com
    login: "{{ gitlab_com_login }}"
    password: "{{ gitlab_com_password }}"

research_db_docker_registries:
  - url: registry.gitlab.com
    login: "{{ gitlab_com_login }}"
    password: "{{ gitlab_com_password }}"
